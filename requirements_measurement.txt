# Requirements for Floor Plan Measurement System
# Install with: pip install -r requirements_measurement.txt

# Core dependencies
numpy>=1.15.0
scipy>=1.1.0
opencv-python>=3.4.0
matplotlib>=2.2.0

# Deep learning (choose one based on your setup)
# For GPU:
# tensorflow-gpu==1.15.0
# For CPU:
# tensorflow==1.15.0

# Image I/O (for Python 3 compatibility)
imageio>=2.5.0
Pillow>=6.0.0

# PDF processing (for working with PDF floor plans)
pdf2image>=1.14.0
# Note: Also requires poppler system package:
#   Mac: brew install poppler
#   Ubuntu/Debian: sudo apt-get install poppler-utils
#   Windows: Download from https://github.com/oschwartz10612/poppler-windows/releases/

# OCR for automatic scale extraction from PDFs
pytesseract>=0.3.0
# Note: Also requires tesseract system package:
#   Mac: brew install tesseract
#   Ubuntu/Debian: sudo apt-get install tesseract-ocr
#   Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki
